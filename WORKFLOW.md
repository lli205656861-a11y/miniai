# תהליך העבודה — קורס AI לצוות מרכז הרכב שמעון ברזילי

מסמך זה מתעד את **כל התהליך**, מקצה לקצה, כדי שכל שלב יהיה ברור ומתועד בקבצים.
עיקרון מנחה: *כמנהלי הקורס — כל תהליך חייב להיות מתועד בקבצים בצורה ברורה.*

## 1. סקירה כללית
```
עובד ממלא אבחון (barziminiai.netlify.app)
        │
        ├─▶ מסך תעודה מעוצב (ניתן להורדה)               [חוויית העובד]
        │
        └─▶ שליחה אוטומטית ל-Apps Script (/exec)         [איסוף]
                 │
                 ├─▶ שורה בגיליון "משוב אבחון AI"         [תמונת-על קבוצתית]
                 │
                 └─▶ תיק עובד בדרייב:                     [תיק אישי]
                         עובדי הקורס — אבחון AI/
                             └── {שם העובד}/
                                   ├── אבחון רמת AI — {תאריך} (מסמך)
                                   └── תעודת מוכנות AI — {שם} — {תאריך}.png
```

## 2. מבנה התיקיות בדרייב
- **תיקיית הקורס** (התיקייה המשותפת) — מכילה את כל החומרים.
  - **`משוב אבחון AI`** (גיליון) — יומן מרכזי: שורה לכל אבחון של כל עובד. לניתוח קבוצתי והחלטות על תוכן הקורס.
  - **`עובדי הקורס — אבחון AI`** (תיקייה, נוצרת אוטומטית) — מכילה תת-תיקייה לכל עובד.
    - **`{שם העובד}`** (תיקייה, נוצרת אוטומטית באבחון הראשון) — **תיק העובד** לאורך כל הקורס:
      - `אבחון רמת AI — {תאריך} (מזהה)` — מסמך אישי מלא (ציון, רמה, פירוט ידע, כל התשובות).
      - `תעודת מוכנות AI — {שם} — {תאריך}.png` — התעודה המעוצבת.

## 3. שלבי הקורס
| שלב | מה קורה | מה מתועד ואיפה |
| --- | --- | --- |
| **פתיחה** | העובד ממלא את האבחון | תיק עובד נפתח + מסמך אבחון פתיחה + תעודה · שורה בגיליון |
| **במהלך הקורס** | נוכחות, תרגילים, הערות מנחה | מוסיפים קבצים לתוך תיקיית העובד (ידני) |
| **סיום** | העובד ממלא את אותו אבחון שוב | מסמך + תעודה חדשים (מתוארכים) נשמרים באותה תיקייה · שורה נוספת בגיליון |
| **הפקת לקחים** | השוואת פתיחה↔סיום פר עובד, ותמונה קבוצתית בגיליון | הכל כבר מתועד — אין צורך באיסוף ידני |

## 4. מדדים שנאספים (לכל אבחון)
- **מדד מוכנות (0–100)** ו**רמה (1–5)**.
- **ידע / ניסיון / מוכנות** באחוזים.
- **פירוט ידע לפי דרגה** (בסיסי / בינוני / מתקדם).
- כל התשובות, כולל הטקסטים החופשיים על כאבים וציפיות, ונכונות כל שאלת ידע.
- מזהה אבחון + חותמת זמן + זמן מילוי.

## 5. איך משתמשים בזה להצלחת הקורס
- **לפני הקורס:** מהגיליון — מזהים פערי ידע נפוצים ומתאימים את התכנים.
- **פר עובד:** תיק העובד מרכז את כל הסיפור שלו — נוח למנחה.
- **בסוף:** משווים תעודת פתיחה מול תעודת סיום — עלייה מדידה להצגה להנהלה ולעובד.

## 6. רכיבים טכניים (למי שמתחזק)
- `index.html` — האתר + האבחון + מחולל התעודה (Canvas, ללא ספריות חיצוניות).
- `apps-script/Code.gs` — האיסוף לדרייב (גיליון + תיק עובד + תעודה). ראה `SETUP-DRIVE.md`.
- `netlify.toml` — פרסום סטטי. האתר החי: https://barziminiai.netlify.app/
