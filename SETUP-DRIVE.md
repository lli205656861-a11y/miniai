# חיבור הדף לגיליון המשוב בדרייב (הקמה חד־פעמית, ~דקה)

המטרה: שכל אבחון שעובד ממלא בדף יישמר **אוטומטית כשורה** בגיליון המשוב שבתיקיית הדרייב שלך — כדי שתוכלי לראות את כל התשובות במקום אחד ולהסיק מסקנות על תכני הקורס.

**גיליון המשוב כבר נוצר עבורך בתיקייה:**
📄 *"משוב אבחון AI — מרכז הרכב שמעון ברזילי"*
https://docs.google.com/spreadsheets/d/16RJdyAhl83WWhIRusFsdZAouTrqnP47sT9AjfOmfE40/edit

## שלב 1 — הדבקת הסקריפט
1. פתחי את גיליון המשוב (הקישור למעלה).
2. בתפריט: **Extensions → Apps Script** (הרחבות → Apps Script).
3. מחקי את מה שכתוב שם, והדביקי במקום את כל התוכן של הקובץ [`apps-script/Code.gs`](apps-script/Code.gs).
4. לחצי על אייקון השמירה (💾).

## שלב 2 — פרסום כ־Web App
1. למעלה מימין: **Deploy → New deployment** (פריסה → פריסה חדשה).
2. ליד "Select type" לחצי על גלגל השיניים ובחרי **Web app**.
3. הגדרות:
   - **Execute as:** `Me` (אני)
   - **Who has access:** `Anyone` (כל אחד)  ← חשוב, כדי שהדף יוכל לשלוח
4. **Deploy** → יבקשו ממך לאשר הרשאות → אשרי (Allow) עם חשבון הגוגל שלך.
5. יופיע **Web app URL** שנראה כך:
   `https://script.google.com/macros/s/AKfy.../exec`
   **העתיקי אותו.**

## שלב 3 — חיבור הדף
1. פתחי את הקובץ `index.html`.
2. חפשי בראש בלוק ה־`<script>` את השורה:
   ```js
   const SUBMIT_URL = "";
   ```
3. הדביקי בין המרכאות את הכתובת שהעתקת:
   ```js
   const SUBMIT_URL = "https://script.google.com/macros/s/AKfy.../exec";
   ```
4. שמרי והעלי מחדש את הדף (או פרסמי מחדש את התצוגה המקדימה).

## זהו! ✅
מעכשיו כל עובד שמסיים את האבחון — שורה חדשה נכנסת אוטומטית ללשונית **"תשובות"** בגיליון:
שם, תפקיד, מדד המוכנות, הרמה, פירוט הידע (בסיסי/בינוני/מתקדם), כל התשובות, וזמן המילוי.

**בדיקה:** מלאי את האבחון פעם אחת ורעֲנני את הגיליון — השורה שלך אמורה להופיע.

### הערות
- הדף שולח בשקט ברקע. אם אין חיבור/הכתובת ריקה — כפתורי **שליחה במייל / העתקה / הורדה** ממשיכים לעבוד כרגיל.
- רוצה עמודות בשמות אחרים או סדר אחר? זה נקבע במפתחות של הפונקציה `buildRecord` ב־`index.html`.
- כל שינוי בסקריפט מצריך **Deploy → Manage deployments → עריכה → New version**.
