# חיבור השאלון לגיליון המשוב בדרייב (הקמה של דקה)

המטרה: שכל אבחון שעובד ממלא יישמר **אוטומטית כשורה** בגיליון
**"משוב אבחון AI — מרכז הרכב שמעון ברזילי"** שכבר קיים בתיקיית הדרייב שלך.
כך כל התשובות מתרכזות במקום אחד ואפשר להסיק מהן מסקנות לגבי תכני הקורס.

## שלב 1 — להדביק את הסקריפט
1. פותחים בדרייב את הגיליון **"משוב אבחון AI — מרכז הרכב שמעון ברזילי"**.
2. בתפריט למעלה: **Extensions ▸ Apps Script** (תוספים ▸ Apps Script).
3. מוחקים את הקוד שמופיע כברירת מחדל, ומדביקים את כל התוכן של הקובץ
   [`apps-script/Code.gs`](apps-script/Code.gs).
4. לוחצים על אייקון השמירה (💾).

## שלב 2 — לפרסם כ-Web App
1. למעלה מימין: **Deploy ▸ New deployment**.
2. ליד "Select type" בוחרים **Web app**.
3. מגדירים:
   - **Execute as:** `Me` (החשבון שלך)
   - **Who has access:** `Anyone`
4. לוחצים **Deploy**, מאשרים הרשאות (Authorize access ▸ בוחרים חשבון ▸ Allow).
5. מעתיקים את הכתובת שמופיעה תחת **Web app URL** (מסתיימת ב-`/exec`).

## שלב 3 — לחבר לדף
פותחים את `index.html`, בראש בלוק ה-JavaScript, ומדביקים את הכתובת:

```js
const SUBMIT_URL = "https://script.google.com/macros/s/XXXXXXXX/exec";
```

זהו. מרגע זה כל עובד שמסיים אבחון — התשובות והציון שלו נכתבים אוטומטית
כשורה חדשה בגיליון. אפשר גם לשלוח לי את הכתובת ואשלב אותה בשבילך.

## מה נשמר בכל שורה
חותמת זמן, שם, תפקיד, מדד מוכנות, רמה, אחוזי ידע/ניסיון/מוכנות,
פירוט הידע לפי דרגה (בסיסי/בינוני/מתקדם), נכונות כל שאלת ידע,
וכל התשובות המלאות — כולל הטקסטים החופשיים על כאבים וציפיות.

## הערות
- אם `SUBMIT_URL` נשאר ריק, הדף פשוט מדלג על השליחה האוטומטית
  ונשאר עם שליחה במייל / העתקה / הורדה (אין שגיאה).
- בדף המתארח (Artifact/אתר) — לאחר שמזינים את `SUBMIT_URL`, צריך
  לפרסם מחדש את הקובץ המעודכן.

---

## עדכון: תיק עובד אישי לאורך הקורס
הסקריפט המעודכן (`apps-script/Code.gs`) מוסיף, בכל אבחון שמושלם, גם:
- **תת-תיקייה על שם העובד** בתוך תיקייה בשם *"עובדי הקורס — אבחון AI"* (נוצרת אוטומטית בתיקיית הקורס).
- **מסמך אבחון אישי** בתוך תיקיית העובד — ציון, רמה, פירוט ידע וכל התשובות. זו נקודת ההתחלה של תיק העובד; לאורך הקורס אפשר להוסיף לאותה תיקייה נוכחות, תרגילים, ואבחון סיום (שיישמר כמסמך מתוארך נוסף באותה תיקייה).

### כדי להחיל את העדכון (חשוב — שמירה על אותה כתובת)
1. פותחים את ה-Apps Script של הגיליון, מחליפים את כל הקוד בגרסה החדשה מ-`apps-script/Code.gs`, ושומרים.
2. **Deploy ▸ Manage deployments ▸** (עיפרון ✏️ ליד הפריסה הקיימת) **▸ Version: New version ▸ Deploy**.
   > חשוב לעדכן את הפריסה הקיימת (ולא ליצור פריסה חדשה) — כך כתובת ה-`/exec` נשארת זהה ואין צורך לשנות דבר באתר.
3. בפעם הראשונה תתבקש **לאשר הרשאות דרייב** (Authorize ▸ Allow) — כי עכשיו הסקריפט יוצר תיקיות וקבצים.

---

## אישור הרשאות דרייב (חובה — בגלל הרשאות מצטברות)
אם האבחון נכתב לגיליון אבל **לא נוצרות תיקיות/מסמכים/תעודות** — הסיבה כמעט תמיד
שהרשאות Drive/Docs לא אושרו (אישור דרך `doGet` לא מספיק, כי הוא לא משתמש בהן).
**הפתרון:** בעורך בחר את הפונקציה **`setup`** ▸ **Run ▶** ▸ אשר את ההרשאות (Allow).
ההרשאה נשמרת ברמת החשבון — הפריסה הקיימת תתחיל לעבוד מיד, בלי redeploy.
