# סביבת ניהול הקורס — `admin.html`

דשבורד ניהולי למנהלי הקורס. תמונת-על על כל המשתתפים במקום אחד, ושליטה בחשיפת המפגשים —
הכל נמשך אוטומטית מהדרייב (גיליון האבחון + טאב "מעקב קורס"), ללא הזנה ידנית.

## כתובת וכניסה
- הכתובת: `‎https://barziminiai.netlify.app/admin.html`
- הכניסה מוגנת ב**סיסמת מנהל** שנבדקת **בשרת** (Apps Script) — היא לא שמורה בקוד הדף,
  והנתונים נטענים רק אחרי אימות מוצלח.
- הסיסמה מוגדרת ב-`apps-script/Code.gs` במשתנה `MANAGER_PW` (ברירת מחדל: `ברזילי-ניהול-2026`).
  מומלץ לשנות אותה לערך פרטי ולפרוס מחדש (גרסה חדשה של אותה פריסה).

## מה יש בדשבורד
- **תמונת מצב** — מספר משתתפים, מדד מוכנות ממוצע, אחוז התקדמות ממוצע, כמה התחילו וכמה השלימו את הקורס.
- **חשיפת מפגשים** — מתגי פתיחה/סגירה לכל מפגש. "שמור חשיפה" מעדכן את השרת, וכל המשתתפים
  רואים את המצב החדש בטעינה הבאה של מרחב הקורס (`course.html` מושך את מצב החשיפה מהשרת בכל כניסה).
  כך אין צורך לערוך קוד כדי לפתוח מפגש.
- **טבלת משתתפים** — לכל עובד: מוכנות AI בתצוגת **לפני → אחרי** עם דלתא (השינוי מתחילת הקורס),
  רמה, אחוז התקדמות בקורס, מפגשים ותרגולים שהושלמו, מועד עדכון אחרון, וקישור ישיר לתיק העובד בדרייב.
- **חיפוש/סינון** (כולם / עשו אבחון / רשומים לקורס / השלימו) ו**ייצוא CSV**.

## איך זה מתחבר לנתונים
- **אבחון** (`index.html`) → שורה בגיליון + תיק עובד. הדשבורד מקבץ את כל האבחונים לפי שם:
  הראשון = "לפני", האחרון = "אחרי" (בסיס להשוואת לפני/אחרי בסוף הקורס).
- **קורס** (`course.html`) → טאב "מעקב קורס". הדשבורד ממזג לפי שם העובד.
- הצד השרת: `doGet?action=admin&pw=…` מחזיר את הרשומות הממוזגות; `doGet?action=release` מחזיר את
  מצב החשיפה; `doPost {type:'release'}` (עם סיסמה) שומר אותו ב-Script Properties.

> אחרי כל עדכון ל-`Code.gs` יש לפרוס מחדש כגרסה חדשה של אותה פריסה כדי לשמור על אותה כתובת `/exec`.
